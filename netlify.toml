[build]
  command = "npm install && chmod +x ./node_modules/.bin/vite && npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA routing: send all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200